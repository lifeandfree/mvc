<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security.xsd">

    <beans:bean name="saveUserInSession" class="ru.innopolis.security.SaveUserInSessionHandler"/>

    <http auto-config="true" request-matcher="ant">
        <!--security zone-->
        <intercept-url pattern="/first" access="hasAnyRole('ROLE_USER')"/>
        <intercept-url pattern="/hello" access="isAuthenticated()"/>

        <!--public zone-->
        <intercept-url pattern="/" access="permitAll()"/>
        <intercept-url pattern="/login" access="permitAll()"/>
        <intercept-url pattern="/reg" access="permitAll()"/>
        <intercept-url pattern="/resources" access="permitAll()"/>
        <intercept-url pattern="favicon.png" access="permitAll()"/>
        <csrf disabled="true"/>
        <form-login username-parameter="login"
                    password-parameter="password"
                    login-processing-url="/perform_login"
                    authentication-failure-url="/login?error"
                    default-target-url="/first"
                    authentication-success-handler-ref="saveUserInSession"/>
        <logout logout-success-url="/?logout" logout-url="/perform_logout"/>
    </http>

    <beans:bean class="ru.innopolis.security.StorePasswordEncoder" name="encoder"/>
    <beans:bean class="ru.innopolis.security.AuthenticationProvider" name="authenticationProvider"/>

    <authentication-manager>
        <authentication-provider user-service-ref="authenticationProvider">
            <password-encoder ref="encoder"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>